<section class="content-header">
    <h1>
        @yield('contentheader_title', 'Header not found')
        <small>@yield('contentheader_description')</small>
    </h1>
    @yield('breadcrumb_title', 'Breadcrumb not found')
</section>